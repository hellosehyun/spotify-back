type In = {}
type Out = Promise<{}>

export const createPlaylist = () => ({
  execute: async (params: In): Out => {
    const {} = pre(params)
    return {}
  },
})

const pre = (params: In) => {
  return {}
}
